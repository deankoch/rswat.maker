# outputs: a lookup table for land use codes and a table of STATSGO download links
# path_out = list(land_lookup = file.path(dev_dir, 'nlcd_swat_code.csv'),
#                 usgs_lookup = file.path(dev_dir, 'usgs_param_code.csv'),
#                 links = file.path(dev_dir, 'statsgo_links.csv'))
